Program UnoA;
Const
  NumEjecuciones = 3;
Var
  i ,numero : Integer;
  sumaDeNumeros : Integer;

Begin
  sumaDeNumeros := 0;
  Write('Introduzca 3 numeros positivos o negativos y ');
  Write('Calcular‚ su suma: ');
  For i:=1 To NumEjecuciones Do
    Begin
      Readln(numero);
      sumaDeNumeros:=SumaDeNumeros + Numero;
    End;
  Writeln;
  Writeln('La suma de los numeros es = ',sumaDeNumeros);
  Readln;
End.